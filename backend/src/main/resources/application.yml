server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/color_prediction
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

app:
  jwt:
    secret: "c3VwZXItc2VjcmV0LWxvbmctYmFzZTY0LXN0cmluZw=="
    expirationMs: 86400000
  twilio:
    enabled: false
    accountSid: ""
    authToken: ""
    fromNumber: "+10000000000"
  cors:
    allowed-origins: "http://localhost:4200"

